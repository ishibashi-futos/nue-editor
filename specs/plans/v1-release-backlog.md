# v1 Release Backlog（Layer3）

`specs/plans/v1-release-plan.md` の Layer3（タスク単位の粒度）を、Issue/ToDo 登録と進捗チェック用に分離したファイル。

- 参照元: `specs/plans/v1-release-plan.md`
- 対象タスク: `T-001` 〜 `T-037`
- 運用: タスク完了時にタスク行と受け入れ条件をチェックする

## タスク一覧（チェックリスト）

## L1-1

### SS-1-1

- [x] T-001（変更対象: `specs`（クレート外））
  - 関連ステップ: `L1-1 / SS-1-1`
  - 作業内容の要約:
    - ローカル起動スコープ（必須画面、必須操作、許容モック範囲、スモーク手順）を `specs/plans/v1-release-plan.md` 冒頭に固定し、実装判断のぶれをなくす。
  - 受け入れ条件:
    - [x] 実装者が「このマイルストーンで何を完成扱いにするか」を文書だけで判断できる
  - 依存タスク: なし

### SS-1-2

- [x] T-002（変更対象: `nue-app`）
  - 関連ステップ: `L1-1 / SS-1-2`
  - 作業内容の要約:
    - `nue-app/src/main.rs` を `Hello, world!` から実起動処理へ置換し、起動引数解析・設定解決・`tokio` ランタイム初期化・UI起動呼び出し・エラー終了コードを実装する。
  - 受け入れ条件:
    - [x] `cargo run -p nue-app -- --help` 相当のヘルプ/引数検証が動く
    - [x] `Hello, world!` 出力が消える
    - [x] `tokio` を前提とした起動骨格が確立される
  - 依存タスク: `T-001`

- [x] T-003（変更対象: `nue-config`）
  - 関連ステップ: `L1-1 / SS-1-2`
  - 作業内容の要約:
    - `add()` 雛形を起動設定用の最小 API（CLI 引数からの最小設定値注入、既定値、将来拡張余地）に置き換え、`nue-app` から参照できる型を整備する。
    - 本マイルストーンでは設定ファイル読み込み・環境変数マージ・ホットリロードはスコープ外とし、必要ならスタブで置く。
  - 受け入れ条件:
    - [x] `nue-app` で型安全に起動設定を受け取れる
    - [x] `nue-config` が雛形のままではなく起動に意味を持つ
    - [x] 設定ファイル読み込み/ホットリロード未実装（スタブ可）でも本マイルストーンの DoD 判定が可能
  - 依存タスク: `T-002`

### SS-1-3

- [x] T-004（変更対象: `nue-ui`）
  - 関連ステップ: `L1-1 / SS-1-3`
  - 作業内容の要約:
    - GPUI 起動用の公開 API（例: `run_app(...)`）を新設し、`nue-app` から UI を起動できるようにする。GPUI 実 API への適合を優先し、先行モジュール分割は最小限に留める。
  - 受け入れ条件:
    - [x] `nue-app` から `nue-ui` の単一入口を呼び出せる
    - [x] UI 起動責務が `nue-app` と `nue-ui` で明確に分離される
  - 依存タスク: `T-002`

- [x] T-005（変更対象: `nue-ui`）
  - 関連ステップ: `L1-1 / SS-1-3`
  - 作業内容の要約:
    - GPUI ルートビュー（空状態/プレースホルダ表示）を追加し、ウィンドウ生成と初回描画を成立させる。実接続後に必要な分だけモジュール分割を見直す前提で進める。
  - 受け入れ条件:
    - [ ] `macOS` 上で `cargo run -p nue-app` により GPUI ウィンドウが表示される
    - [ ] クラッシュせずプレースホルダ文字列または画面枠が見える
  - 依存タスク: `T-004A`

- [x] T-006（変更対象: `nue-ui`）
  - 関連ステップ: `L1-1 / SS-1-3`
  - 作業内容の要約:
    - `DesignSystem` のフォント資産/カラーパレットを GPUI 起動時に登録・適用するアダプタ層を追加し、実画面で利用可能にする。
    - フォントは仕様書定義に合わせて `include_bytes!` によるバイナリ埋め込み方式で登録する。
  - 受け入れ条件:
    - [ ] `include_bytes!` で埋め込んだフォントの登録処理が起動時に実行される
    - [ ] ルート画面で DesignSystem 由来のスタイルを利用できる
  - 依存タスク: `T-005`

## L1-2

### SS-2-1

- [x] T-007（変更対象: `nue-app`）
  - 関連ステップ: `L1-2 / SS-2-1`
  - 作業内容の要約:
    - App Host 相当の統合状態（ワークスペース一覧、アクティブワークスペースID、画面/パネル種別、通知、セッション一覧）を定義する。
  - 受け入れ条件:
    - [x] 空状態と 1 セッション状態を同一型で表現できる
    - [x] UI 側から読み書きしやすい責務分割になっている
  - 依存タスク: `T-005`

- [x] T-008（変更対象: `nue-app`）
  - 関連ステップ: `L1-2 / SS-2-1`
  - 作業内容の要約:
    - ワークスペースセッション単位の状態束（editor/explorer/command hub/tab/terminal/subscribers 等）を保持する構造体を定義する。
  - 受け入れ条件:
    - [x] セッション生成/破棄をアプリ層で管理できる
    - [x] 複数ワークスペース対応へ拡張可能な形になっている
  - 依存タスク: `T-007`

### SS-2-2

- [x] T-009（変更対象: `nue-core`）
  - 関連ステップ: `L1-2 / SS-2-2`
  - 作業内容の要約:
    - `LegacyWorkspaceEditor` から `EditorCore` の高度機能（イベント排出、ショートカット登録等）を扱えるよう、必要最小限の委譲 API と読み取りアクセサを追加する（生 `&mut EditorCore` は公開しない）。
  - 受け入れ条件:
    - [x] アプリ層が `LegacyWorkspaceEditor` を使いながら `EditorCore` のイベントと入力導線を利用できる
    - [x] 二重状態（別 `EditorCore` を持つ）を作らずに済む
    - [x] ワークスペース境界チェック（ファイル選択/保存責務）が `LegacyWorkspaceEditor` に残る
  - 依存タスク: `T-008`

- [ ] T-010（変更対象: `nue-app`）
  - 関連ステップ: `L1-2 / SS-2-2`
  - 作業内容の要約:
    - `WorkspaceRegistry` とセッション生成を接続し、ワークスペース追加/選択時に `LegacyWorkspaceEditor`・UIコントローラ群・ターミナル実行基盤（tokio/PTY接続の受け口）を初期化するファクトリ処理を実装する。
  - 受け入れ条件:
    - [ ] ワークスペースのパスからセッションを生成できる
    - [ ] 失敗時にエラー通知へ変換できる
  - 依存タスク: `T-004A`, `T-008`, `T-009`

- [ ] T-011（変更対象: `nue-app`）
  - 関連ステップ: `L1-2 / SS-2-2`
  - 作業内容の要約:
    - アクティブワークスペース選択状態（`WorkspaceRegistry` とは別責務）を実装し、Rail 表示・セッション表示と同期する。
  - 受け入れ条件:
    - [ ] 複数登録されたワークスペースのうち、どれを表示中かをアプリ層で一意に管理できる
  - 依存タスク: `T-010`

### SS-2-3

- [ ] T-012（変更対象: `nue-ui`）
  - 関連ステップ: `L1-2 / SS-2-3`
  - 作業内容の要約:
    - ワークスペースセッション画面の基本レイアウト（Rail/左パネル/Editor/Terminal/Overlay枠）を GPUI で描画するビューを作成する。
  - 受け入れ条件:
    - [ ] セッション状態を渡すと基本レイアウトが表示される
    - [ ] 画面要素の位置関係が安定している
  - 依存タスク: `T-006`, `T-008`

- [ ] T-013（変更対象: `nue-ui`）
  - 関連ステップ: `L1-2 / SS-2-3`
  - 作業内容の要約:
    - `Workspace Rail` の実ビューを追加し、`WorkspaceRegistry` の一覧・ステータス・追加/除外導線を描画に反映する。
  - 受け入れ条件:
    - [ ] ワークスペース一覧とステータスが表示される
    - [ ] 追加/除外 UI 操作がアプリ層へ通知される
  - 依存タスク: `T-012`

- [ ] T-014（変更対象: `nue-ui`）
  - 関連ステップ: `L1-2 / SS-2-3`
  - 作業内容の要約:
    - 左パネルコンテナ（Explorer/Search/VCS 切替）と未実装パネル用プレースホルダビューを追加し、パネル遷移の受け皿を作る。
  - 受け入れ条件:
    - [ ] Explorer/Search/VCS を切り替えると表示領域が変わる
    - [ ] Search/VCS 未実装でもクラッシュしない
  - 依存タスク: `T-012`

## L1-3

### SS-3-1

- [ ] T-015（変更対象: `nue-ui`）
  - 関連ステップ: `L1-3 / SS-3-1`
  - 作業内容の要約:
    - `LegacyExplorerModel` を使う Explorer 実ビューを実装し、ノード選択イベントをアプリ層へ通知する。
  - 受け入れ条件:
    - [ ] ファイル/ディレクトリ一覧が表示される
    - [ ] ファイル選択イベントが発火する
  - 依存タスク: `T-012`, `T-013`

- [ ] T-016（変更対象: `nue-app`）
  - 関連ステップ: `L1-3 / SS-3-1`
  - 作業内容の要約:
    - Explorer の選択イベントを `LegacyWorkspaceEditor::select_file` に接続し、選択結果をエディタ表示状態・タブ名へ反映する。
  - 受け入れ条件:
    - [ ] Explorer からファイル選択で内容表示が切り替わる
    - [ ] 失敗時は通知/メッセージ表示になる
  - 依存タスク: `T-010`, `T-015`

### SS-3-2

- [ ] T-017（変更対象: `nue-ui`）
  - 関連ステップ: `L1-3 / SS-3-2`
  - 作業内容の要約:
    - 中央エディタの最小入力ビュー（テキスト表示/編集）を実装し、アプリ層へキー入力/カーソル/保存イベントを渡せるようにする。
  - 受け入れ条件:
    - [ ] テキスト入力と再描画が可能
    - [ ] 保存ショートカット/ボタンイベントが送出される
  - 依存タスク: `T-012`, `T-016`

- [ ] T-018（変更対象: `nue-app`）
  - 関連ステップ: `L1-3 / SS-3-2`
  - 作業内容の要約:
    - `EditorInputController` 相当の入力導線を `LegacyWorkspaceEditor` の委譲 API 経由で接続し、エディタ入力・ショートカット・保存処理を統合する（アプリ層で `EditorCore` を直接保持しない）。
    - エディタ外でのファイル変更検知と自動リロード（File System Watcher 連携）は本マイルストーンの対象外とする。
  - 受け入れ条件:
    - [ ] 編集操作で `LegacyWorkspaceEditor` 内部の `EditorCore` が更新される
    - [ ] 保存操作でファイルが実際に更新される
  - 依存タスク: `T-009`, `T-017`

- [ ] T-019（変更対象: `nue-core`）
  - 関連ステップ: `L1-3 / SS-3-2`
  - 作業内容の要約:
    - 起動時導線で使いやすいように、`LegacyWorkspaceEditor` 経由の委譲 API を Tidy First で整理する（入力系、イベント drain、状態参照系を責務ごとに分割し、生アクセサ公開を避ける）。
  - 受け入れ条件:
    - [ ] `nue-app` が `LegacyWorkspaceEditor` を壊さずに編集導線を組める
    - [ ] 追加 API の責務が明確でテスト可能
    - [ ] `LegacyWorkspaceEditor` が巨大な単一Facadeにならないよう機能群が整理されている
  - 依存タスク: `T-009`

### SS-3-3

- [ ] T-020（変更対象: `nue-app`）
  - 関連ステップ: `L1-3 / SS-3-3`
  - 作業内容の要約:
    - `LegacyWorkspaceEditor` の委譲 API 経由で `EditorCore` イベントを定期/入力後に drain し、`EditorEventSubscriber` と UI 表示状態（通知、Minimap件数、SmartGutter件数等）へ反映する。
    - 本マイルストーンではアプリ内操作由来のイベント反映を対象とし、エディタ外変更検知を起点にした再読み込みイベント連携は扱わない。
  - 受け入れ条件:
    - [ ] 編集・保存・差分同期イベント後に UI 表示が更新される
    - [ ] イベントが重複処理されない
  - 依存タスク: `T-018`

- [ ] T-021（変更対象: `nue-ui`）
  - 関連ステップ: `L1-3 / SS-3-3`
  - 作業内容の要約:
    - Minimap/SmartGutter/StructurePath の高度描画前段として、件数・フォーカス・直近ジャンプ/承認情報を表示する軽量パネル/バッジ UI を追加する。
  - 受け入れ条件:
    - [ ] `EditorEventSubscriber` の状態変化が画面で視認できる
    - [ ] 後続の本描画実装に置き換えやすい
  - 依存タスク: `T-020`

## L1-4

### SS-4-1

- [ ] T-022（変更対象: `nue-ui`）
  - 関連ステップ: `L1-4 / SS-4-1`
  - 作業内容の要約:
    - Command Hub オーバーレイ実ビュー（入力欄、候補一覧、確認状態、通知表示）を実装し、`CommandHubUiController` と接続する。
  - 受け入れ条件:
    - [ ] 開閉・入力・候補選択・確認が UI 上で操作できる
    - [ ] 通知が表示される
  - 依存タスク: `T-012`

- [ ] T-023（変更対象: `nue-app`）
  - 関連ステップ: `L1-4 / SS-4-1`
  - 作業内容の要約:
    - Command Hub 開閉ショートカット、ESC、外側クリックのイベントをアプリ全体入力として処理し、オーバーレイ状態とフォーカスを統合管理する。
  - 受け入れ条件:
    - [ ] Command Hub を任意タイミングで開閉できる
    - [ ] フォーカス競合で入力が暴走しない
  - 依存タスク: `T-022`

### SS-4-2

- [ ] T-024（変更対象: `nue-app`）
  - 関連ステップ: `L1-4 / SS-4-2`
  - 作業内容の要約:
    - `CommandHubActionModel` の初期状態（workspaces/panes/terminals/tabs/focused_panel）を実アプリ状態から構築し、画面更新時に再同期する処理を作る。
  - 受け入れ条件:
    - [ ] Command Hub 候補一覧が実際の画面状態に追従する
    - [ ] 二重管理の不整合が抑制される
  - 依存タスク: `T-011`, `T-012`, `T-022`

- [ ] T-025（変更対象: `nue-app`）
  - 関連ステップ: `L1-4 / SS-4-2`
  - 作業内容の要約:
    - `CommandActionEvent`（panel/pane/tab/workspace/terminal）をアプリ状態へ反映するディスパッチャを実装し、画面遷移と内部状態を一致させる。
  - 受け入れ条件:
    - [ ] `panel focus` で Explorer/Search/VCS が切り替わる
    - [ ] `pane/tab` 系コマンドで UI 状態が変化する
  - 依存タスク: `T-024`

- [ ] T-026（変更対象: `nue-ui`）
  - 関連ステップ: `L1-4 / SS-4-2`
  - 作業内容の要約:
    - パネル切替 UI（Explorer/Search/VCS タブ/ボタン）を実装し、Command Hub と同じ画面遷移状態を共有する。
  - 受け入れ条件:
    - [ ] UI 操作でも Command Hub と同等にパネルが切り替わる
    - [ ] 遷移状態が単一ソースで管理される
  - 依存タスク: `T-014`, `T-025`

- [ ] T-031（変更対象: `nue-app`）
  - 関連ステップ: `L1-4 / SS-4-2`
  - 作業内容の要約:
    - `CommandHubStateSink`（または同等の更新点）を使って、Command Hub 実行後のスナップショット同期を一元化し、状態ずれを減らす。
  - 受け入れ条件:
    - [ ] Command Hub 実行後に workspaces/panes/tabs/terminals/focused_panel が一貫して更新される
  - 依存タスク: `T-024`, `T-025`

### SS-4-3

- [ ] T-027（変更対象: `nue-ui`）
  - 関連ステップ: `L1-4 / SS-4-3`
  - 作業内容の要約:
    - `TabBarUiController` と連携したタブバー実ビューを作成し、選択・並び替え・クローズ等の操作イベントを発火できるようにする。
  - 受け入れ条件:
    - [ ] タブ一覧が表示され、最低限のタブ操作を UI から実行できる
  - 依存タスク: `T-012`

- [ ] T-028（変更対象: `nue-app`）
  - 関連ステップ: `L1-4 / SS-4-3`
  - 作業内容の要約:
    - `TabBarUiController` / `PaneManager` のイベントをアプリ状態に接続し、タブ・ペイン表示と Editor 表示の整合を取る。
  - 受け入れ条件:
    - [ ] UI/Command Hub のどちらからのタブ・ペイン操作でも画面が破綻しない
  - 依存タスク: `T-025`, `T-027`

- [ ] T-029（変更対象: `nue-ui`）
  - 関連ステップ: `L1-4 / SS-4-3`
  - 作業内容の要約:
    - ターミナル領域ビューを実装し、`TerminalUiController` と `TerminalScrollbackRenderer` の状態（キュー、監査、スクロールバック）を表示する。
  - 受け入れ条件:
    - [ ] ターミナル領域でキュー/監査メッセージ/出力行が表示される
  - 依存タスク: `T-012`

- [ ] T-030（変更対象: `nue-app`）
  - 関連ステップ: `L1-4 / SS-4-3`
  - 作業内容の要約:
    - `macOS` 限定で、ターミナル操作導線を `TerminalSession` と実コマンド実行（PTY）へ接続し、Zed で採用実績のある `alacritty_terminal` を許容して `run_command` の起動・出力取り込み・完了/失敗反映を `tokio` 上で実装する。
    - 将来の `Windows` 対応を見据え、端末制御の土台は `alacritty_terminal` 前提で組み立てる。
  - 受け入れ条件:
    - [ ] ターミナル UI 上で実コマンド実行の状態遷移（Queued/Running/Completed/Failed）を確認できる
    - [ ] 実行出力がスクロールバックへ反映される
    - [ ] 終了コード/失敗を監査イベントと UI 表示に反映できる
  - 依存タスク: `T-002`, `T-004A`, `T-010`, `T-029`

## L1-5

### SS-5-1

- [ ] T-032（変更対象: `workspace-root`（クレート外））
  - 関連ステップ: `L1-5 / SS-5-1`
  - 作業内容の要約:
    - `scripts/run_nue.sh`（仮名）を追加し、`cargo run -p nue-app` と起動引数・前提チェック（絶対パス）をラップする。
  - 受け入れ条件:
    - [ ] スクリプトからワークスペース指定で起動できる
    - [ ] ヘルプ/エラーメッセージが日本語で分かる
  - 依存タスク: `T-002`, `T-010`

- [ ] T-033（変更対象: `nue-app`）
  - 関連ステップ: `L1-5 / SS-5-1`
  - 作業内容の要約:
    - `--workspace` 指定時にワークスペース自動登録・自動選択・初期セッション起動を行う導線を実装し、起動直後確認を簡単にする。
  - 受け入れ条件:
    - [ ] コマンド一発でセッション画面まで到達できる
  - 依存タスク: `T-010`, `T-011`

### SS-5-2

- [ ] T-034（変更対象: `nue-app`, `nue-ui`, `nue-core`）
  - 関連ステップ: `L1-5 / SS-5-2`
  - 作業内容の要約:
    - 統合アプリ状態やイベントディスパッチャに対する単体/統合テスト（GUI非依存）を追加し、主要画面遷移のロジックを自動検証する。
  - 受け入れ条件:
    - [ ] 主要遷移（workspace選択、panel切替、command hub反映）のロジックがテストで再現できる
  - 依存タスク: `T-025`, `T-031`

- [ ] T-035（変更対象: `specs`（クレート外））
  - 関連ステップ: `L1-5 / SS-5-2`
  - 作業内容の要約:
    - `specs/plans/v1-release-plan.md` に `macOS` 限定の GUI 手動スモークテスト手順（空起動時の未選択画面、`--workspace` 起動、ファイル選択、編集、保存、Command Hub、パネル切替、Terminal `run_command` 実行）を追記し、DoD 判定手順を固定する。
  - 受け入れ条件:
    - [ ] 実行者が同じ手順で動作確認できる
  - 依存タスク: `T-033`, `T-034`

- [ ] T-036（変更対象: `workspace-root`（クレート外））
  - 関連ステップ: `L1-5 / SS-5-2`
  - 作業内容の要約:
    - 最終チェックとして `cargo fmt`, `cargo check --all-targets`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, 必要に応じて `scripts/sanity.sh` を実行し、`macOS` 限定 GUI スモーク結果とあわせて結果記録を行う。
  - 受け入れ条件:
    - [ ] 最低限の品質ゲートが全て通過している
    - [ ] 失敗時は原因と再現手順（OS を含む）が記録される
  - 依存タスク: `T-034`

### SS-5-3

- [ ] T-037（変更対象: `specs`（クレート外））
  - 関連ステップ: `L1-5 / SS-5-3`
  - 作業内容の要約:
    - 本マイルストーンの既知制約（Search/VCSプレースホルダ、PTY 実装の制限事項、MCP/Semantic未接続など）を明記し、次フェーズの実装バックログへ引き継ぐ。
  - 受け入れ条件:
    - [ ] 達成範囲と未達範囲が文書上で誤解なく区別できる
  - 依存タスク: `T-035`, `T-036`
