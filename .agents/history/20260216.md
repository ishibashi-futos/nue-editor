# Dashboard

## 📋 基本情報
- **最終更新日時**: 2026-02-16 16:12 (JST)
- **現在のステータス**: 作業完了
- **現在のブランチ**: `feat/v1.0`

---

## ⚠️ 要対応（User Action Required）
> ユーザーの判断や入力が必要な項目です。
- [ ] 今回は特に追加対応はありません

---

## 🚀 スキル化・自動化候補（DRY candidates）
> 繰り返しの操作や、複雑なコマンドを簡略化するアイディア。

---

## 🏆 成果報告（Activity Log）
### [review.md 対応: Minimap再同期判定・更新イベント再通知・Markdown見出しカウント修正]
- **変更の理由**: `review.md` の Mid 3件で、Minimap再同期時の誤 `FocusNotFound`、編集系更新時の Minimap/Smart Gutter イベント欠落、Markdown見出し数の誤検知（コードブロック内 `#`）が指摘されたため。
- **解決アプローチ**:
  - Red: `nue-core/src/editor_core.rs` と `nue-core/src/markdown_service.rs` に失敗テストを追加し、再同期時 `Synced` 維持、Undo時の更新イベント再発火、フェンスコードブロック内 `#` 除外を固定化。
  - Green: `nue-core/src/editor_core.rs` で `sync_minimap_focus_id` を `has_focus` 事前判定に変更し、同一 `focus_id` 再同期ではイベントなしでも `Synced` を返すよう修正。`minimap_service` / `smart_gutter_service` の `on_buffer_updated` を変更差分がある場合のみ更新イベントを返す設計に変更し、`EditorCore` で編集/Undo/Redo 後に再通知する経路を追加。`markdown_service` の見出し数計算をフェンスコードブロック対応に修正。
  - Refactor/検証: `cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行し全通過を確認。
- **影響範囲**:
  - `nue-core/src/editor_core.rs`
  - `nue-core/src/minimap_service.rs`
  - `nue-core/src/smart_gutter_service.rs`
  - `nue-core/src/markdown_service.rs`
- **関連リソース**:
  - review.md
- **ToDo事項**:
  - なし

### [ED-7 Smart Gutter 差分判別・ジャンプ・Approval 連携イベントの実装]
- **変更の理由**: `ED-7` 要件である `Smart Gutter` の差分タイプ判別（AI/Git）、色アニメーション、クリックジャンプ、`Approval Request` 連携、および `Structure Path` / `Command Hub` と連動するイベント定義が未実装だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/editor_core.rs` に失敗テストを追加し、`Smart Gutter` の差分登録・`focus_id` 同期・クリックジャンプ・`Approval Request` オープンの期待挙動を固定。
  - Green: `nue-core/src/smart_gutter_service.rs` を新規追加し、差分状態管理（AI/Git 判別とアニメーション）、`focus_id` 同期、ジャンプ要求、`Approval Request` 連携イベント生成を実装。`EditorCore` に `SmartGutterService` を組み込み、公開 API と `EditorCoreEvent` への橋渡しを追加。
  - Refactor/検証: `scripts/sanity.sh` を実行し、`fmt/check/clippy/test` の全通過を確認。
- **影響範囲**:
  - `nue-core/src/smart_gutter_service.rs`（新規）
  - `nue-core/src/editor_core.rs`
  - `nue-core/src/lib.rs`
  - `specs/backlog.md`（ED-7 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `nue-ui` 側で `EditorCoreEvent::SmartGutter*` を購読し、gutter 描画/クリックハンドラ/Command Hub 遷移を接続する

### [ED-6 Minimap と Shadow Buffer オーバーレイ基礎の実装]
- **変更の理由**: `ED-6` 要件である Minimap 基盤と、検索結果/AI差分/Git差分のライン・色オーバーレイ、および `focus_id` 更新時の `Command Hub` / `Structure Path` 同期イベントが未実装だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/editor_core.rs` に失敗テストを追加し、オーバーレイ更新と `focus_id` 同期イベントの期待挙動を固定。
  - Green: `nue-core/src/minimap_service.rs` を新規追加し、オーバーレイ状態管理（検索/AI/Git）、行数同期、`focus_id` 同期イベントを実装。`EditorCore` に `MinimapService` を組み込み、`update_minimap_overlays` / `sync_minimap_focus_id` / `minimap_snapshot` API とイベント橋渡しを追加。
  - Refactor/検証: 未使用 import を整理し、`cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行して全通過を確認。
- **影響範囲**:
  - `nue-core/src/minimap_service.rs`（新規）
  - `nue-core/src/editor_core.rs`
  - `nue-core/src/lib.rs`
  - `specs/backlog.md`（ED-6 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `nue-ui` 側で `EditorCoreEvent::MinimapOverlaysUpdated` / `EditorCoreEvent::MinimapFocusIdSynced` を購読し、Minimap と Structure Path の実表示更新に接続する

### [ED-5 MarkdownService 分割と差分/同期イベント追加]
- **変更の理由**: `ED-5` 要件である Markdown リッチ機能の責務分割（構文強調・リスト補完・ペア補完・リンク開く・プレビュー同期）と、差分検知/同期を観測するイベント導線が未実装だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/editor_core.rs` に失敗テストを追加し、Markdownサブ機能要求、Markdown編集時の差分検知イベント、プレビュー同期イベント、非Markdownファイル時の拒否を固定化。
  - Green: `nue-core/src/markdown_service.rs` を新規追加し、`MarkdownFeature` ごとの要求処理と、編集差分から `MarkdownDiffObserved` / `MarkdownPreviewSynced` を生成する実装を追加。`EditorCore` に `MarkdownService` を組み込み、編集/Undo/Redo 時にイベントを委譲・発火する経路を追加。
  - Refactor/検証: `clippy::ptr_arg` 指摘に合わせて `&PathBuf` を `&Path` に整理。`cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行し全通過を確認。
- **影響範囲**:
  - `nue-core/src/markdown_service.rs`（新規）
  - `nue-core/src/editor_core.rs`
  - `nue-core/src/lib.rs`
  - `specs/backlog.md`（ED-5 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `nue-ui` 側で Markdown 操作 UI を `EditorCore::execute_markdown_feature` に接続し、`MarkdownFeatureRequested` / `MarkdownDiffObserved` / `MarkdownPreviewSynced` を表示更新へ反映する

### [ED-4 右クリックメニュー基盤と EditorCore メニュー実行導線の実装]
- **変更の理由**: `ED-4` 要件である「右クリックメニュー基盤」と「保存/コピー/Markdown メニューから `Editor Core` API を呼び出す導線」が未実装だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/editor_core.rs` に失敗テストを追加し、右クリックメニューの開閉、保存メニュー実行、コピー/Markdown メニュー実行、未オープン時の拒否挙動を固定化。
  - Green: `EditorCore` に `EditorContextMenu` 状態、`open_context_menu`、`execute_context_menu_item` を追加。保存は `SaveTrigger::ContextMenu` で既存 `request_save` に委譲し、コピー/Markdown は `CopyRequested` / `MarkdownMenuRequested` イベントを発火する実装を追加。
  - Refactor/検証: `open_file` 時のメニュー状態リセットを追加して状態整合を単純化。`cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行し全通過を確認。
- **影響範囲**:
  - `nue-core/src/editor_core.rs`
  - `specs/backlog.md`（ED-4 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
- **ToDo事項**:
  - `nue-ui` 側で右クリックイベントを `EditorCore::open_context_menu` / `execute_context_menu_item` に接続し、メニュー表示へ反映する

### [ED-3 ショートカット収集・登録機構と実行コマンド拡張]
- **変更の理由**: `ED-3` 要件である `Cmd/Ctrl + S/Z/P/F/Shift+F` 等のショートカット登録機構と、各ショートカットから `EditorCore` コマンドを実行する経路が未完了だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/editor_core.rs` に失敗テストを追加し、既定ショートカットの一括登録、`Cmd/Ctrl + P/F/Shift+F` 実行、`Cmd/Ctrl + Z` と `Cmd+Shift+Z` / `Ctrl+Y` による Undo/Redo を固定化。
  - Green: `EditorCommand` / `CommandExecutionOutcome` に `QuickOpen` / `FindInFile` / `FindInWorkspace` を追加し、`register_default_shortcuts` と既定バインディング収集関数を実装。`dispatch_shortcut` から各コマンドを返す経路を追加。
  - Refactor/検証: ショートカット収集の重複呼び出しを整理し、`./scripts/sanity.sh` 実行で `fmt/check/clippy/test` 全通過を確認。
- **影響範囲**:
  - `nue-core/src/editor_core.rs`
  - `specs/backlog.md`（ED-3 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
- **ToDo事項**:
  - `nue-ui` 入力レイヤーから `EditorCore::dispatch_shortcut` へ接続する UI 実装を追加

### [review.md 対応: Legacy Workspace 保存安全性とファイルツリー耐障害性の修正]
- **変更の理由**: `review.md` で指摘された High 2件（非アトミック保存、保存時ワークスペース境界未検証）と Mid 2件（部分I/O失敗で全体失敗、`to_string_lossy` によるパス劣化）を解消するため。
- **解決アプローチ**:
  - `LegacyWorkspaceEditor` の保存処理を `tmp + rename` のアトミック保存へ変更し、保存直前に `canonicalize` + `is_inside_workspace` を再実行して境界を再検証。
  - `EditorCore` と `LegacyFileTree` / `LegacyWorkspaceEditor` の `file_path` / `absolute_path` を `String` から `PathBuf` に変更し、非UTF-8パスの情報劣化を防止。
  - `LegacyFileTree` の再帰走査で、子要素単位の `read_dir` / `file_type` / 再帰失敗をスキップして継続するよう変更し、部分的アクセス不能でワークスペース全体が開けなくなる挙動を改善。
  - 追加テストとして「保存直前シンボリックリンク差し替えの拒否」と「子ディレクトリアクセス失敗時の継続」を実装。
  - `cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行し、全通過を確認。
- **影響範囲**:
  - `nue-core/src/legacy_workspace_editor.rs`
  - `nue-core/src/legacy_file_tree.rs`
  - `nue-core/src/editor_core.rs`
- **関連リソース**:
  - review.md
- **ToDo事項**:
  - `PathBuf` へ変更した `EditorCore` の公開型を使う上位層（UI連携時）で、表示用文字列化ポリシーを明示化する

### [ED-2 ファイル選択から EditorCore 基本操作までの導線実装]
- **変更の理由**: `ED-2` 要件である「ファイル選択時に `EditorCore` でバッファを開き、表示・編集・保存・Undo/Redo を可能にする」導線が未実装だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/legacy_workspace_editor.rs` を新規作成し、`select_file` でのバッファオープン、編集後保存、Undo/Redo、ワークスペース外ファイル拒否の失敗テストを先に追加。
  - Green: `LegacyWorkspaceEditor` を実装し、`select_file` で canonical path の境界検証とファイル読込を行って `EditorCore::open_file` に接続。`save_active_file` で `request_save` -> ファイル書き込み -> `mark_saved` を一貫実行。
  - Refactor/検証: macOS の `/var` と `/private/var` 差異を避けるためテスト比較を canonicalize 基準へ整理し、`cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行して全通過を確認。
- **影響範囲**:
  - `nue-core/src/legacy_workspace_editor.rs`（新規）
  - `nue-core/src/lib.rs`
  - `specs/backlog.md`（ED-2 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `nue-ui` 側の Legacy View ファイル選択イベントを `LegacyWorkspaceEditor::select_file` へ接続

### [ED-1 Legacy View ファイルツリー基盤の実装]
- **変更の理由**: `ED-1` 要件である「ワークスペース内のファイル一覧をツリー構造でレンダリングする」ための基盤モデルが未実装だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/legacy_file_tree.rs` を新規追加し、ルート配下のファイル/ディレクトリをツリー化できること、存在しないルート、ファイル指定ルートの失敗を検証するテストを先に追加。`build` は未実装エラーで意図的に失敗させた。
  - Green: `LegacyFileTree::build` にディレクトリ再帰走査を実装し、`LegacyFileTreeNode`（Directory/File）を構築。子要素は「ディレクトリ優先 + 名前順」で安定ソートする実装を追加。
  - Refactor/検証: 冗長分岐と Clippy 指摘を整理し、`scripts/sanity.sh`（fmt/check/clippy/test）を実行して全通過を確認。
- **影響範囲**:
  - `nue-core/src/legacy_file_tree.rs`（新規）
  - `nue-core/src/lib.rs`
  - `specs/backlog.md`（ED-1 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `nue-ui` 側で `LegacyFileTree` を購読して Explorer 描画コンポーネントへ接続

### [WS-3 WorkspaceRail ステータスのライブ更新連携]
- **変更の理由**: `WS-3` 要件である「Busy/Waiting/Error/Idle の色+アニメーション表現」と「基盤サーバ状態と連携したライブ更新」が、`WorkspaceRegistry` と `DesignSystem` 間で明示的に接続されていなかったため。
- **解決アプローチ**:
  - Red: `nue-core/src/workspace_registry.rs` に、サーバ状態更新でイベントを取り出せること・Noop・NotFound を検証する失敗テストを追加。`nue-ui/src/design_system.rs` に `WorkspaceRailState` からスタイル解決の失敗テストを追加。
  - Green: `WorkspaceRegistry` に `update_workspace_status_from_server` / `drain_status_events` と `WorkspaceStatusUpdatedEvent` を追加し、`apply_core_state` 経由でライブ更新イベントをキューに蓄積する実装を追加。`DesignSystem` に `workspace_rail_status_style_for` を追加して `WorkspaceRailState -> AgentStatusStyle` を一元変換。
  - Refactor/検証: `cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行し全通過を確認。
- **影響範囲**:
  - `nue-core/src/workspace_registry.rs`
  - `nue-ui/src/design_system.rs`
  - `specs/backlog.md`（WS-3 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `nue-ui` の WorkspaceRail 実コンポーネントで `drain_status_events` を購読し、描画更新へ接続

### [WS-2 WorkspaceRail 右クリック除外コンテキストメニューの実装]
- **変更の理由**: `WS-2` 要件である「Workspace 項目の右クリック除外」と「除外後の表示更新」が未実装だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/workspace_registry.rs` に、右クリック対象のコンテキストメニュー表示・不正対象拒否・除外実行・除外後一覧更新の失敗テストを追加。
  - Green: `WorkspaceContextMenu` 状態、`open_exclude_context_menu`、`exclude_context_menu_target` を実装し、除外時に `workspaces` から対象を削除してメニュー状態をリセットする処理を追加。
  - Refactor/検証: メニュー終了処理と `workspace_id` 検索を関数分離し、除外後の再追加で `workspace_id` が重複しない採番へ整理。`cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行し全通過を確認。
- **影響範囲**:
  - `nue-core/src/workspace_registry.rs`
  - `specs/backlog.md`（WS-2 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `nue-ui` に WorkspaceRail の右クリックUIを接続し、`WorkspaceRegistry` の新規状態遷移と連動させる

### [review.md 対応: Workspace重複判定/実在チェックとOverlay初期フォーカス修正]
- **変更の理由**: `review.md` で指摘された Mid 3件（同一実体パスの重複登録、無効パス登録、`active_focus_id` 未設定時の背面レイヤー非操作化）を解消し、登録導線とUI操作性の不整合を防ぐため。
- **解決アプローチ**:
  - Red: `nue-core/src/workspace_registry.rs` に「実在しない絶対パスの拒否」「`/./` `..` を含む同一実体パスの重複拒否」テストを追加し、`nue-ui/src/design_system.rs` に「初期フォーカス未設定時も背面レイヤーが操作可能」テストを追加。
  - Green: `WorkspaceRegistry` に `canonicalize_path` を導入し、絶対パス・実在/アクセス可否・ディレクトリ種別を検証。重複判定と登録値を canonical path 基準へ統一。
  - Green: `focus_matches` を `None/None` 一致も許容する仕様へ変更し、初期状態で `SmartGutter/Decoration/Minimap` がインタラクティブになるよう修正。
  - Refactor/検証: `cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行し全通過を確認。
- **影響範囲**:
  - `nue-core/src/workspace_registry.rs`
  - `nue-ui/src/design_system.rs`
- **関連リソース**:
  - review.md
  - specs/spec-nue.md
- **ToDo事項**:
  - UI側で `WorkspacePathValidation::NotFoundOrInaccessible` / `MustBeDirectory` のメッセージ文言を明示マッピング

### [WS-1 ワークスペース登録ダイアログフローの実装]
- **変更の理由**: `WS-1` 要件である「`+` からの追加ダイアログ操作（パス入力→検証→追加）」と `WorkspaceRail` 反映の導線が未実装だったため。
- **解決アプローチ**:
  - Red: `nue-core/src/workspace_registry.rs` を新規追加し、ダイアログ開閉・パス検証（空/相対/重複）・追加後の `WorkspaceRailModel` 反映を検証する失敗テストを先に実装。
  - Green: `WorkspaceRegistry` に `open_add_dialog` / `update_dialog_path` / `submit_add` を実装し、絶対パスのみを受理して `workspace-<n>` ID と display name を生成、`WorkspaceRailModel` を追加する処理を実装。
  - Refactor/検証: パス正規化と表示名抽出を関数分離し、`scripts/sanity.sh`（fmt/check/clippy/test）を実行して全通過を確認。
- **影響範囲**:
  - `nue-core/src/workspace_registry.rs`（新規）
  - `nue-core/src/lib.rs`
  - `specs/backlog.md`（WS-1 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `WS-2` で Workspace 項目右クリック除外の UI/状態遷移を接続

### [DS-5 オーバーレイ z-index マネージャーの実装]
- **変更の理由**: `DS-5` 要件である `Command Hub > Smart Gutter > Decoration > Minimap` の重なり順を `focus_id` と連動して統一管理する仕組みが未実装だったため。
- **解決アプローチ**:
  - Red: `nue-ui/src/design_system.rs` に z-index 優先度、`Command Hub` 操作時の背面無効化、`focus_id` 一致/不一致での Smart Gutter 透明度を検証する失敗テストを先に追加。
  - Green: `OverlayLayer` / `OverlayFocusState` / `OverlayLayerStyle` / `OverlayZIndexManager` を追加し、`DesignSystem::overlay_zindex_manager` から UI が参照できるよう実装。
  - Refactor/検証: Clippy 指摘に合わせて `Default` 実装を derive 化し、`scripts/sanity.sh`（fmt/check/clippy/test）全通過を確認。
- **影響範囲**:
  - `nue-ui/src/design_system.rs`
  - `specs/backlog.md`（DS-5 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `ED-7` / `ED-6` 実装時に `OverlayZIndexManager::resolve` を描画レイヤー制御へ接続

### [DS-4 差分承認ステータスのカラーマッピング整備]
- **変更の理由**: `DS-4` 要件である未承認/承認済み差分の色規約（Solar Flare / Electric Lime / Cloud White）を `SmartGutter` / `Approval UI` / `Gutter` で共通利用できる設計が未実装だったため。
- **解決アプローチ**:
  - Red: `nue-ui/src/design_system.rs` に失敗テストを先に追加し、各 Surface ごとの `Pending` / `Approved` / `Neutral` の期待色と強調度を固定化。
  - Green: `DiffSurface` / `DiffState` / `DiffStyle` と `DesignSystem::diff_style_for` を追加し、色決定と強調度決定を専用関数に分離して実装。
  - Refactor/検証: `cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` と `scripts/sanity.sh` の両方を実行し、全通過を確認。
- **影響範囲**:
  - `nue-ui/src/design_system.rs`
  - `specs/backlog.md`（DS-4 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `ED-7` 実装時に `diff_style_for` を `Smart Gutter` の描画ロジックへ接続

### [DS-3 Agent Status スタイル定義の整備]
- **変更の理由**: `DS-3` 要件のうち、`Command Hub` / `Structure Path` / `Workspace Rail` で共通参照できる Agent Status のスタイル定義（遷移タイミング・Glow 含む）が不足していたため。
- **解決アプローチ**:
  - Red: `nue-ui/src/design_system.rs` に `status_style_for` の失敗テストを先に追加し、3 画面向けの期待値（色・アニメーション・遷移時間・Glow・Idle デュアルトーン）を固定化。
  - Green: `StatusSurface` / `AgentStatusStyle` / `TransitionTiming` / `GlowSpec` を追加し、`DesignSystem::status_style_for` から UI 別に再利用可能な規約を返す実装を追加。
  - Refactor/検証: ルール計算を `transition_timing_for` と `glow_spec_for` に分離し、`scripts/sanity.sh` で `fmt/check/clippy/test` の全通過を確認。
- **影響範囲**:
  - `nue-ui/src/design_system.rs`
  - `specs/backlog.md`（DS-3 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `WS-3` / `ED-13` 実装時に `status_style_for` を接続し、表示部品側でライブ更新を有効化

### [DS-2 Glass 共通スタイルの StyleObject 化]
- **変更の理由**: `DS-2` 要件の「Glass 質感を Command Hub/主要パネルで再利用できるスタイル定義」が未整備で、利用先ごとの重複実装を防ぐ共通 API が必要だったため。
- **解決アプローチ**:
  - Red: `nue-ui/src/design_system.rs` に `glass_style_for_surface` の失敗テストを追加し、`CommandHub` と `PrimaryPanel` のプリセット値を先に固定化。
  - Green: `GlassSurface` と `GlassStyleObject` を追加し、`DesignSystem::glass_style_for_surface` で用途別の Glass スタイルを返す実装を追加。
  - Refactor/検証: 初期化重複を `glass_style_from_material` に集約し、`scripts/sanity.sh` で `fmt/check/clippy/test` の全通過を確認。
- **影響範囲**:
  - `nue-ui/src/design_system.rs`
  - `specs/backlog.md`（DS-2 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `DS-3` で Agent Status スタイル定義を Command Hub / Workspace Rail / Structure Path へ接続

### [review.md 対応: Command Hub 破壊的候補誤選択とキャンセル状態の識別修正]
- **変更の理由**: `review.md` の High/Mid 指摘により、破壊的コマンドで target 指定を無視して別対象を選び得る問題と、キャンセル時に `Closed`/`BackToListing` を判別できない問題を解消する必要があったため。
- **解決アプローチ**:
  - Red: `nue-core/src/command_hub_actions.rs` に失敗テストを追加し、`workspace remove`/`pane close`/`terminal kill` の target 指定時に候補が1件へ絞られること、キャンセル通知が `Closed` と `BackToListing` で分岐されることを固定化。
  - Green: `CommandHubActionModel::candidates_for` を target 指定時にドメインごとの検索関数（`workspace_index_by_target` など）で候補を1件に絞る実装へ変更。
  - Green: `CommandHubDispatchOutcome` を `Closed` / `BackToListing` へ分割し、`dispatch_cancel_action` が Picker の状態遷移を潰さず返すように変更。
  - Refactor/検証: `scripts/sanity.sh` を実行し、`fmt/check/clippy/test` の全通過を確認。
- **影響範囲**:
  - `nue-core/src/command_hub_actions.rs`
- **関連リソース**:
  - review.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `CommandHubDispatchOutcome` の分岐を利用する `nue-ui` 側で、キャンセル後の表示状態（モーダル閉鎖/一覧復帰）を明示的に描き分ける

### [CH-4 破壊的操作の確認・失敗/キャンセル通知経路の実装]
- **変更の理由**: `CH-4` 要件である「破壊的操作の確認フロー」と「Command Hub 実行時の失敗/キャンセル通知」のうち、キャンセル時に対象候補を通知する経路と、確認後実行の失敗通知をまとめる経路が未整備だったため。
- **解決アプローチ**:
  - `nue-core/src/command_hub.rs` の `PickerCancelOutcome` に `candidate_id` を持たせ、確認中キャンセル/一覧キャンセルのどちらでも取り消し対象を返せるようにした。
  - `CommandHubSession` に `confirm_selected_candidate` を追加し、確認ステップを明示的に呼び出せるようにした。
  - `should_open_picker` を見直し、`list` に加えて `remove` / `close` / `kill` でも Picker を開くようにして、破壊的操作の2段階実行フローへ到達できるようにした。
  - `nue-core/src/command_hub_actions.rs` に `CommandHubDispatchOutcome` と `dispatch_selected_action` / `dispatch_confirmed_action` / `dispatch_cancel_action` を追加し、`NeedsConfirmation` / `Failed` / `Canceled` を単一路線で返せるようにした。
  - Red として失敗テスト2件を追加し、Green で実装、Refactor 後に `scripts/sanity.sh` と `cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行して通過確認した。
- **影響範囲**:
  - `nue-core/src/command_hub.rs`
  - `nue-core/src/command_hub_actions.rs`
  - `specs/backlog.md`（CH-4 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - Command Hub 実行結果（`Failed`/`Canceled`）を `nue-ui` の通知表示へ接続する UI 側実装

### [CH-3 Workspace/Panes/Panel/Terminal/Selected コマンド実装]
- **変更の理由**: `CH-3` 要件にあるドメイン別コマンドの候補取得と実行モデルが未実装で、`Action Mode` から具体的な操作へ接続できなかったため。
- **解決アプローチ**:
  - `nue-core/src/command_hub_actions.rs` を新規追加し、`CommandHubActionModel` で `Workspace` / `Pane` / `Panel` / `Terminal` / `Selected` のコマンド実行を一元管理できるようにした。
  - `List` 系に対応する `candidates_for` を追加し、Picker が使える `PickerCandidate` をドメインごとに生成するようにした（Workspace/Pane/Terminal）。
  - `execute` で `Add/List/Remove/Split/Next/Prev/Close/Open` 相当の振る舞いを実装し、`Selected Open` では `https://` URL と path を判定、解決不能時は理由付きエラーを返すようにした。
  - Red として失敗テスト4件を先に追加し、Green で最小実装、最後に `scripts/sanity.sh` と `cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` を実行して確認した。
- **影響範囲**:
  - `nue-core/src/command_hub_actions.rs`（新規）
  - `nue-core/src/lib.rs`（モジュール公開）
  - `specs/backlog.md`（CH-3 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `CH-4` の破壊的操作時の確認フローと、失敗/キャンセル通知の UI 経路を接続

### [DS-1 Neon-Night-Glass パレットと FontContext 登録]
- **変更の理由**: `DS-1` 要件のうち、`JetBrainsMono-Regular/Bold/Italic` の埋め込みカタログ管理と仕様値どおりのカラーパレット定義が不足していたため。
- **解決アプローチ**:
  - `nue-ui/src/design_system.rs` に `BundledFont` と `FontContext::from_bundled_fonts` / `bundled_fonts()` を追加し、フォントキー・スタイル・ライセンス・バイト列を一元管理できるようにした。
  - `nue-ui/assets/fonts/` に `JetBrainsMono-Regular.ttf` / `JetBrainsMono-Bold.ttf` / `JetBrainsMono-Italic.ttf` と `OFL.txt` を追加し、`include_bytes!` でバイナリ埋め込みに切り替えた。
  - カラーパレットに `Ether Purple` を追加し、`Cloud White` / `Dusty Grey` を仕様値へ補正。ユニットテストでパレット値と埋め込みカタログの存在を固定化した。
  - `scripts/sanity.sh` を実行して `fmt/check/clippy/test` を通過確認した。
- **影響範囲**:
  - `nue-ui/src/design_system.rs`
  - `nue-ui/assets/fonts/JetBrainsMono-Regular.ttf`
  - `nue-ui/assets/fonts/JetBrainsMono-Bold.ttf`
  - `nue-ui/assets/fonts/JetBrainsMono-Italic.ttf`
  - `nue-ui/assets/fonts/OFL.txt`
  - `specs/backlog.md`（DS-1 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `DS-2` の Glass 共通スタイル適用先（Command Hub/主要パネル）の整理

### [CH-2 Action/Navigation 切替と Picker 連動]
- **変更の理由**: `CH-2` 要件に対して、構文解析と Picker が別々に存在しており、モード切替時の選択/実行/キャンセルを一貫して扱う経路が不足していたため。
- **解決アプローチ**:
  - `nue-core/src/command_hub.rs` に `CommandHubSession` を追加し、入力文字列・解析結果・Picker状態を同一モデルで扱えるようにした。
  - `CommandPicker` に `select_next`/`select_previous` を実装し、候補移動を循環選択で処理できるようにした。
  - `Navigation` または `Action+List` のときのみ Picker を開く判定を導入し、`Enter` で実行・`Esc` でキャンセル可能な導線をテスト先行で固定化した。
  - Red/Green 実施後に `cargo fmt --all && cargo check --all-targets && cargo clippy --all-targets -- -D warnings && cargo test --quiet` と `scripts/sanity.sh` を実行して検証した。
- **影響範囲**:
  - `nue-core/src/command_hub.rs`
  - `specs/backlog.md`（CH-2 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `CH-3` の各ドメインコマンド実行（Workspace/Panes/Panel/Terminal/Selected）の実体連携

### [CH-1 Command Hub 起動/終了トリガー制御]
- **変更の理由**: `Cmd/Ctrl + Shift + P` での起動、ランチャークリック起動、ESC/外側クリック終了の制御モデルが未定義で、Command Hub の表示トリガー要件を満たせていなかったため。
- **解決アプローチ**:
  - `nue-core/src/command_hub.rs` に `CommandHubVisibilityController` を追加。
  - `CommandHubKeyInput`（`CmdShiftP`/`CtrlShiftP`/`Escape`）と開閉理由（`Shortcut`/`LauncherClick`/`EscapeKey`/`OutsideClick`）を型として定義。
  - Red として開閉トリガーの失敗テストを追加し、Green で最小実装、`scripts/sanity.sh` と Rust 総合チェックで検証。
- **影響範囲**:
  - `nue-core/src/command_hub.rs`
  - `specs/backlog.md`（CH-1 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md
- **ToDo事項**:
  - `CH-2` の Action/Navigation モード切替と Picker 実行経路の統合を進める

### [CH-5 Command Hub Literal target パーサ対応]
- **変更の理由**: Command Hub の `target` が常に正規化されており、仕様にある Literal バイパス（`"..."` / `--literal`）を満たしていなかったため。
- **解決アプローチ**:
  - `nue-core/src/command_hub.rs` の Action Mode 解析を分割し、`target` 解釈を専用関数に集約。
  - `target` が `"..."` 形式の場合は中身をそのまま保持する分岐を追加。
  - `--literal`（大文字小文字無視）を受理し、以降の文字列を非正規化で保持する分岐を追加。
  - 失敗テストを先に追加し、Green で最小実装後に `scripts/sanity.sh` と Rust 総合チェックを実行。
- **影響範囲**:
  - `nue-core/src/command_hub.rs`
  - `specs/backlog.md`（CH-5 完了チェック）
- **関連リソース**:
  - specs/backlog.md
  - specs/roadmap.md
  - specs/spec-nue.md

### [review.md 対応: B-1/B-2/B-5/B-6 指摘修正]
- **変更の理由**: `.github/review.md` で指摘された MUST 要件未充足と境界値テスト不足を解消し、仕様整合と保守性を回復するため。
- **解決アプローチ**:
  - `focus_id` を連番から ULID 形式へ変更し、同一ターゲットでの再利用を導入。
  - `latency_ms == 50` の境界テストを追加。
  - `WorkspaceRail` から UI 専用型を除去し、`nue-core` がコア状態型のみを扱うよう整理。
  - `TerminalSession` に `queue_max_pending` 未設定時の既定値 4 を実装（`new` 追加、`new_with_queue_max_pending` へ分離）。
  - `EditorCore` の `request_save` (`NoBuffer`/`NotDirty`) と `mark_saved` stale revision のテストを追加。
- **影響範囲**:
  - `nue-core/src/focus_layer.rs`
  - `nue-core/src/workspace_rail.rs`
  - `nue-core/src/terminal_session.rs`
  - `nue-core/src/editor_core.rs`
- **関連リソース**:
  - .github/review.md
  - specs/spec-nue.md

### [B-6 EditorCore 基本モジュールの整備]
- **変更の理由**: Markdown/Editor の基礎操作（バッファ編集、Undo/Redo、保存、ショートカット）の共通モデルが未実装で、上位機能が連携するフック地点が不足していたため。
- **解決アプローチ**: `nue-core` に `editor_core` モジュールを追加し、ファイルオープン、カーソル移動、挿入編集、Undo/Redo、保存要求/保存確定、ショートカット登録/実行を一元管理するモデルを実装。`drain_events` で `BufferOpened/BufferEdited/SaveRequested/Saved/Shortcut*` を取得できるようにし、ユニットテスト先行で振る舞いを固定化。
- **影響範囲**: `nue-core` 公開 API（`editor_core` 追加）、`specs/backlog.md` の B-6 完了反映。
- **関連リソース**:
  - specs/backlog.md
  - specs/spec-nue.md

### [B-2 focus_id 統一管理レイヤーの整備]
- **変更の理由**: ペイン・タブ・Command Hub・ターミナル間でフォーカス状態を一貫して扱う基盤がなく、同期遅延やイベント追跡の観測点が未定義だったため。
- **解決アプローチ**: nue-core に focus_layer モジュールを追加し、FocusTarget（Pane/Tab/CommandHub/Terminal）を統一表現として定義。enqueue -> start -> finish の同期キューと focus_id 連番発行、drain_events による Queued/SyncStarted/Synced 監査イベント、50ms 予算判定を実装し、テスト先行で固定化。
- **影響範囲**: nue-core 公開 API（focus_layer 追加）、specs/backlog.md の B-2 完了反映。
- **関連リソース**:
  - specs/backlog.md
  - specs/spec-nue.md

### [B-5 TerminalSession モデルとイベント API の整備]
- **変更の理由**: run_command のキュー制御と状態通知を扱う中核モデルが未整備で、Terminal UI が Queued/Running/Completed/Failed を一貫表示できなかったため。
- **解決アプローチ**: nue-core に terminal_session モジュールを追加し、FIFO キュー・実行中コマンド管理・完了/失敗遷移・キュー上限超過通知を実装。UI 取得用に drain_events API を定義し、テスト先行で状態遷移と通知を固定化。
- **影響範囲**: nue-core 公開 API（terminal_session 追加）と backlog の進捗管理（B-5 完了）。
- **関連リソース**:
  - specs/backlog.md
  - specs/spec-nue.md

### [B-1 WorkspaceRail 状態遷移モデルの導入]
- **変更の理由**: nue-core にワークスペース状態の基盤モデルが未整備で、UI 同期の起点となるデータ構造が存在しなかったため。
- **解決アプローチ**: WorkspaceRailModel を新規実装し、Busy/Waiting/Error/Idle の状態管理、core/ui 双方向更新、遷移履歴、revision 管理をユニットテスト先行で追加。
- **影響範囲**: nue-core の公開 API（workspace_rail モジュール）と backlog の進捗管理。
- **関連リソース**:
  - specs/backlog.md
  - specs/spec-nue.md
